# Cross-compilation config removed.
# The macOS-specific cross-compile settings (lld-link, xwin paths) were breaking
# native Windows builds. The build-windows.sh script sets these via env vars when needed.
